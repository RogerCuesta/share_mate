---
phase: 02-offline-sync-reliability-core
plan: "05"
type: execute
gap_closure: true
wave: 5
depends_on: ["02-04"]
files_modified:
  - lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart
  - test/core/sync/sync_logger_privacy_test.dart
autonomous: true
requirements: [SYNC-03]
user_setup: []
must_haves:
  truths:
    - "No sensitive raw debug payloads remain in payment/sync remote datasource logging paths (member IDs, amounts, notes, subscription identifiers)."
    - "SubscriptionRemoteDataSource payment/sync telemetry is routed through SyncLogger with sanitized technical metadata."
    - "Automated regression checks fail if sensitive raw debug logging is reintroduced in the targeted datasource path."
  artifacts:
    - "lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart"
    - "test/core/sync/sync_logger_privacy_test.dart"
  key_links:
    - "payment/sync methods in SubscriptionRemoteDataSource emit SyncLogger events instead of direct sensitive debugPrint payloads."
    - "privacy tests enforce both payload sanitization and source-level guardrails for this datasource."
---

<objective>
Close the remaining SYNC-03 privacy logging gap identified in Phase 02 verification.

Purpose: Eliminate sensitive raw debug logging from payment/sync datasource paths and enforce centralized sanitized telemetry.
Output: Targeted logging refactor in `SubscriptionRemoteDataSourceImpl` plus regression checks that block reintroduction.
</objective>

<execution_context>
@/Users/rogerpersonal/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rogerpersonal/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-offline-sync-reliability-core/02-CONTEXT.md
@.planning/phases/02-offline-sync-reliability-core/02-RESEARCH.md
@.planning/phases/02-offline-sync-reliability-core/02-VERIFICATION.md
@.planning/phases/02-offline-sync-reliability-core/02-03-PLAN.md
@.planning/phases/02-offline-sync-reliability-core/02-04-PLAN.md
@lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart
@test/core/sync/sync_logger_privacy_test.dart
</context>

<tasks>
<task type="auto">
  <name>Task 1: Replace sensitive payment/sync debug traces with SyncLogger events</name>
  <files>lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart</files>
  <action>Refactor only the payment/sync-related logging hotspots called out by verification (`updatePaymentStatus`, `updateMemberAmount`, `getPaymentHistory`, plus any adjacent payment/sync trace lines in the same datasource path) so direct `debugPrint` payloads containing raw identifiers or amounts are removed. Route these events through `_syncLogger.logSync` / `_syncLogger.logTerminal` using sanitized technical metadata only (event/action/retry/error class/code/booleans/counts), without changing business behavior, API contracts, or non-gap scope.</action>
  <verify>
    <automated>flutter analyze lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart</automated>
    <automated>bash -lc "rg -n \"_syncLogger\\.log(Sync|Terminal)\\(\" lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart"</automated>
  </verify>
  <done>Targeted payment/sync datasource methods no longer emit raw sensitive values via direct debug logs and use SyncLogger consistently.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression guard for datasource logging privacy</name>
  <files>test/core/sync/sync_logger_privacy_test.dart</files>
  <action>Extend the existing SYNC-03 privacy test suite with a focused source-level guard for `subscription_remote_datasource.dart` that fails if payment/sync logging reintroduces prohibited raw debug patterns (memberId/amount/notes/subscriptionId raw payload traces) and asserts SyncLogger-based logging is present for the targeted payment/sync methods.</action>
  <verify>
    <automated>flutter test test/core/sync/sync_logger_privacy_test.dart</automated>
  </verify>
  <done>Automated privacy checks enforce both sanitization behavior and datasource logging contract for the fixed gap area.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run gap proof checks tied to SYNC-03 verification claim</name>
  <files>lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart, test/core/sync/sync_logger_privacy_test.dart</files>
  <action>Execute targeted static checks mirroring the verification gap criteria: prove no sensitive raw debug logs remain in relevant payment/sync datasource paths and prove SyncLogger telemetry is used instead. Keep checks narrow to this gap to avoid unrelated refactors or broad cleanup.</action>
  <verify>
    <automated>bash -lc "! rg -n \"debugPrint\\(.*(memberId|amount|notes|subscriptionId|Member filter|Amount:)\" lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart"</automated>
    <automated>bash -lc "rg -n \"updatePaymentStatus|updateMemberAmount|getPaymentHistory|_syncLogger\\.log(Sync|Terminal)\" lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart"</automated>
    <automated>flutter test test/core/sync/sync_logger_privacy_test.dart</automated>
  </verify>
  <done>Evidence bundle shows the SYNC-03 privacy gap is closed for payment/sync datasource logging and remains regression-protected.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] flutter analyze lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart
- [ ] flutter test test/core/sync/sync_logger_privacy_test.dart
- [ ] bash -lc "! rg -n \"debugPrint\\(.*(memberId|amount|notes|subscriptionId|Member filter|Amount:)\" lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart"
- [ ] bash -lc "rg -n \"_syncLogger\\.log(Sync|Terminal)\\(\" lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart"
</verification>

<success_criteria>
- All tasks completed
- SYNC-03 privacy logging gap from `02-VERIFICATION.md` is closed in targeted datasource paths
- No sensitive raw debug payloads remain in payment/sync logging path for `SubscriptionRemoteDataSourceImpl`
- SyncLogger is the enforced logging path for the remediated payment/sync methods
- Regression checks protect against future reintroduction of sensitive logs
</success_criteria>

<output>
After completion, create `.planning/phases/02-offline-sync-reliability-core/02-05-SUMMARY.md`
</output>
