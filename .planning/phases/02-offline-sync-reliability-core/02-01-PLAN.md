---
phase: 02-offline-sync-reliability-core
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/sync/payment_sync_queue.dart
  - lib/core/sync/payment_sync_orchestrator.dart
  - lib/core/sync/sync_error_classifier.dart
  - lib/core/di/injection.dart
  - lib/main.dart
  - lib/features/subscriptions/data/repositories/subscription_repository_impl.dart
  - test/core/sync/payment_sync_queue_service_test.dart
  - test/core/sync/payment_sync_orchestrator_test.dart
  - test/core/sync/sync_error_classifier_test.dart
autonomous: true
requirements: [SYNC-01]
user_setup: []
must_haves:
  truths:
    - "Offline payment operations are drained by a single-flight worker with deterministic ordering."
    - "Retry policy is bounded to 5 attempts with exponential backoff + jitter and does not block the rest of the queue."
    - "Terminal operations move out of active retry flow and remain recoverable for manual actions."
  artifacts:
    - "lib/core/sync/payment_sync_orchestrator.dart"
    - "lib/core/sync/payment_sync_queue.dart"
    - "test/core/sync/payment_sync_orchestrator_test.dart"
  key_links:
    - "main.dart bootstrap initializes sync orchestrator and lifecycle triggers."
    - "SubscriptionRepository enqueue path writes to enriched queue records used by orchestrator retries."
---

<objective>
Implement the Phase 2 sync engine foundation for reliable offline queue draining.

Purpose: Close SYNC-01 by adding bounded retry/backoff processing, terminal operation handling, and app lifecycle sync triggers.
Output: Queue model upgrade + orchestrator + wiring in bootstrap/repository + automated reliability tests.
</objective>

<execution_context>
@/Users/rogerpersonal/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rogerpersonal/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-offline-sync-reliability-core/02-CONTEXT.md
@.planning/phases/02-offline-sync-reliability-core/02-RESEARCH.md
@.planning/phases/02-offline-sync-reliability-core/02-VALIDATION.md
@lib/core/sync/payment_sync_queue.dart
@lib/features/subscriptions/data/repositories/subscription_repository_impl.dart
@lib/main.dart
@lib/core/di/injection.dart
</context>

<tasks>
<task type="auto">
  <name>Task 1: Upgrade queue state model for retry scheduling and terminal partitioning</name>
  <files>lib/core/sync/payment_sync_queue.dart, test/core/sync/payment_sync_queue_service_test.dart</files>
  <action>Extend `PaymentSyncOperation` and queue service to support `status` (`pending|processing|terminal`), `nextAttemptAt`, `lastAttemptAt`, `lastErrorClass`, `lastErrorCode`, and terminal metadata. Add service APIs for ordered pending reads, retry updates, terminal transitions, `retryTerminal()`, and `clearTerminalOnly()`. Keep encrypted box usage and backward-compatible deserialization for existing queue rows.</action>
  <verify>
    <automated>flutter test test/core/sync/payment_sync_queue_service_test.dart</automated>
  </verify>
  <done>Queue persistence supports bounded retries and terminal separation without deleting historical failed operations.</done>
</task>

<task type="auto">
  <name>Task 2: Implement single-flight orchestrator with bounded retry/backoff policy</name>
  <files>lib/core/sync/payment_sync_orchestrator.dart, lib/core/sync/sync_error_classifier.dart, test/core/sync/payment_sync_orchestrator_test.dart, test/core/sync/sync_error_classifier_test.dart</files>
  <action>Create `PaymentSyncOrchestrator` with `start/stop/triggerSync` entrypoints, single-flight lock, and deterministic drain order by `createdAt`. Apply retry policy: max 5 attempts, exponential backoff, bounded jitter, and non-blocking continuation when one operation becomes terminal. Add explicit error classification for retryable vs terminal paths and persist retry metadata for each attempt.</action>
  <verify>
    <automated>flutter test test/core/sync/payment_sync_orchestrator_test.dart test/core/sync/sync_error_classifier_test.dart</automated>
  </verify>
  <done>Orchestrator drains queue safely, respects retry ceilings, and prevents duplicate concurrent processors.</done>
</task>

<task type="auto">
  <name>Task 3: Wire sync orchestration to app bootstrap and payment mutation flow</name>
  <files>lib/main.dart, lib/core/di/injection.dart, lib/features/subscriptions/data/repositories/subscription_repository_impl.dart</files>
  <action>Register orchestrator dependencies in DI, initialize/start orchestrator during bootstrap, and trigger sync on app resume, after successful remote payment writes, and on a short throttled foreground periodic interval reconciliation loop (for example every 30-90 seconds with anti-overlap throttle). Ensure repository queue fallback remains optimistic-first while writing enough metadata for orchestrator retries. Keep wiring non-blocking so UI startup and payment toggles remain responsive.</action>
  <verify>
    <automated>flutter analyze lib/main.dart lib/core/di/injection.dart lib/features/subscriptions/data/repositories/subscription_repository_impl.dart lib/core/sync/payment_sync_queue.dart lib/core/sync/payment_sync_orchestrator.dart</automated>
    <automated>bash -lc "rg -n \"maxAttempts|exponential|jitter|singleFlight|triggerSync|foreground|interval|throttle\" lib/core/sync/payment_sync_orchestrator.dart lib/main.dart lib/features/subscriptions/data/repositories/subscription_repository_impl.dart"</automated>
  </verify>
  <done>App resume, post-remote-write, and short throttled foreground periodic reconciliation all invoke the same consistent sync worker path with no manual user intervention required for transient failures.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] flutter test test/core/sync/payment_sync_queue_service_test.dart test/core/sync/payment_sync_orchestrator_test.dart test/core/sync/sync_error_classifier_test.dart
- [ ] flutter analyze lib/main.dart lib/core/di/injection.dart lib/core/sync/payment_sync_queue.dart lib/core/sync/payment_sync_orchestrator.dart lib/features/subscriptions/data/repositories/subscription_repository_impl.dart
- [ ] bash -lc "rg -n \"maxAttempts|retry|nextAttemptAt|terminal|foreground|interval|throttle\" lib/core/sync/payment_sync_queue.dart lib/core/sync/payment_sync_orchestrator.dart lib/main.dart"
</verification>

<success_criteria>
- All tasks completed
- SYNC-01 enforced with 5-attempt bounded retry/backoff and terminal operation handling
- Queue drain is deterministic, single-flight, and lifecycle-triggered
- Retry and terminal transitions are covered by automated sync tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-offline-sync-reliability-core/02-01-SUMMARY.md`
</output>
