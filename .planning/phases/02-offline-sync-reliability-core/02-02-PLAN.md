---
phase: 02-offline-sync-reliability-core
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/core/sync/payment_sync_queue.dart
  - lib/core/sync/payment_sync_conflict_resolver.dart
  - lib/core/sync/payment_sync_orchestrator.dart
  - lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart
  - lib/features/subscriptions/data/repositories/subscription_repository_impl.dart
  - supabase/migrations/20260308_phase2_sync_conflict_reconciliation.sql
  - test/core/sync/conflict_resolution_test.dart
  - test/features/subscriptions/data/repositories/subscription_repository_impl_sync_test.dart
autonomous: true
requirements: [SYNC-02]
user_setup: []
must_haves:
  truths:
    - "Conflict resolution between optimistic local intent and backend monthly reset is deterministic."
    - "Closed-cycle queued operations are converted to business no-op terminal conflicts, never replayed blindly."
    - "Conflict outcomes are auditable through non-PII metadata."
  artifacts:
    - "lib/core/sync/payment_sync_conflict_resolver.dart"
    - "lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart"
    - "test/core/sync/conflict_resolution_test.dart"
  key_links:
    - "Repository enqueue path persists cycle anchor metadata consumed by conflict resolver."
    - "Orchestrator performs conflict preflight before remote mutation."
---

<objective>
Implement deterministic cycle-conflict reconciliation for offline payment operations.

Purpose: Close SYNC-02 by guaranteeing stable convergence when local optimistic writes race against backend monthly reset boundaries.
Output: Conflict resolver + cycle-aware queue metadata + backend/context checks + deterministic no-op terminal path tests.
</objective>

<execution_context>
@/Users/rogerpersonal/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rogerpersonal/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-offline-sync-reliability-core/02-CONTEXT.md
@.planning/phases/02-offline-sync-reliability-core/02-RESEARCH.md
@.planning/phases/02-offline-sync-reliability-core/02-VALIDATION.md
@lib/core/sync/payment_sync_queue.dart
@lib/core/sync/payment_sync_orchestrator.dart
@lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart
@lib/features/subscriptions/data/repositories/subscription_repository_impl.dart
</context>

<tasks>
<task type="auto">
  <name>Task 1: Persist deterministic cycle anchors in queued payment operations</name>
  <files>lib/core/sync/payment_sync_queue.dart, lib/features/subscriptions/data/repositories/subscription_repository_impl.dart, test/features/subscriptions/data/repositories/subscription_repository_impl_sync_test.dart</files>
  <action>Extend enqueue contract to include `cycleDueDate` snapshot and `idempotencyKey` for each queued operation (paid/unpaid flows and bulk variants). Ensure cycle anchor is sourced from member/subscription due-date context at enqueue time and serialized in queue records used by replay logic.</action>
  <verify>
    <automated>flutter test test/features/subscriptions/data/repositories/subscription_repository_impl_sync_test.dart</automated>
  </verify>
  <done>Every queued operation carries cycle metadata required to evaluate stale-cycle conflicts deterministically.</done>
</task>

<task type="auto">
  <name>Task 2: Add conflict preflight resolver in orchestrator replay pipeline</name>
  <files>lib/core/sync/payment_sync_conflict_resolver.dart, lib/core/sync/payment_sync_orchestrator.dart, lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart</files>
  <action>Create a resolver that fetches authoritative backend cycle context before each replay attempt and compares it to operation `cycleDueDate`. If cycle advanced/closed, do not execute remote mutation; instead mark terminal conflict with reason `cycle_conflict_noop`. If cycle matches, execute remote mutation idempotently and treat already-applied remote state as success.</action>
  <verify>
    <automated>flutter test test/core/sync/conflict_resolution_test.dart</automated>
    <automated>bash -lc "rg -n \"cycle_conflict_noop|cycleDueDate|idempotencyKey\" lib/core/sync/payment_sync_conflict_resolver.dart lib/core/sync/payment_sync_orchestrator.dart lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart"</automated>
  </verify>
  <done>Replay behavior is deterministic: stale-cycle operations become terminal no-op conflicts, current-cycle operations apply idempotently.</done>
</task>

<task type="auto">
  <name>Task 3: Add optional backend reconciliation audit plumbing and regression coverage</name>
  <files>supabase/migrations/20260308_phase2_sync_conflict_reconciliation.sql, test/core/sync/conflict_resolution_test.dart</files>
  <action>Add a migration (if required by current schema) for conflict-audit persistence helpers used by resolver/orchestrator, restricted to non-sensitive metadata only. Extend conflict tests to assert audit record creation contract and verify no stale-cycle write reaches business mutation RPC paths.</action>
  <verify>
    <automated>flutter test test/core/sync/conflict_resolution_test.dart test/features/subscriptions/data/repositories/subscription_repository_impl_sync_test.dart</automated>
  </verify>
  <done>Conflict decisions are reproducible and auditable without leaking PII or monetary payloads.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] flutter test test/core/sync/conflict_resolution_test.dart test/features/subscriptions/data/repositories/subscription_repository_impl_sync_test.dart
- [ ] flutter analyze lib/core/sync/payment_sync_queue.dart lib/core/sync/payment_sync_conflict_resolver.dart lib/core/sync/payment_sync_orchestrator.dart lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart lib/features/subscriptions/data/repositories/subscription_repository_impl.dart
- [ ] bash -lc "rg -n \"cycleDueDate|cycle_conflict_noop|idempotencyKey\" lib/core/sync lib/features/subscriptions/data"
</verification>

<success_criteria>
- All tasks completed
- SYNC-02 deterministic convergence rules are enforced for cycle conflicts
- Closed-cycle operations are transformed into terminal no-op conflicts with audit metadata
- Automated tests prove stale-cycle replay does not mutate backend state
</success_criteria>

<output>
After completion, create `.planning/phases/02-offline-sync-reliability-core/02-02-SUMMARY.md`
</output>
