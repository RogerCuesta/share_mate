---
phase: 01-data-safety-access-security
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/settings/data/datasources/account_remote_datasource.dart
  - lib/features/settings/data/repositories/account_repository_impl.dart
  - lib/features/settings/domain/repositories/account_repository.dart
  - lib/features/settings/domain/usecases/delete_account.dart
  - lib/features/settings/presentation/providers/account_actions_provider.dart
  - supabase/functions/delete-account/index.ts
  - supabase/functions/delete-account/deno.json
  - test/features/settings/data/datasources/account_remote_datasource_test.dart
  - test/features/settings/presentation/providers/account_actions_provider_test.dart
autonomous: true
requirements: [SECU-02]
user_setup: []
must_haves:
  truths:
    - "Flutter client never calls Supabase admin delete APIs directly."
    - "Account deletion uses authenticated backend execution and targets only the current JWT user."
    - "Settings delete-account flow still completes and logs user out after success."
  artifacts:
    - "lib/features/settings/data/datasources/account_remote_datasource.dart"
    - "supabase/functions/delete-account/index.ts"
    - "test/features/settings/data/datasources/account_remote_datasource_test.dart"
  key_links:
    - "AccountRemoteDataSource invokes backend delete-account endpoint instead of auth.admin.deleteUser."
    - "Settings UI/provider domain flow no longer requires passing arbitrary userId for deletion."
---

<objective>
Move sensitive account deletion from client-side admin API usage to an authorized backend path.

Purpose: Satisfy SECU-02 by enforcing server-authorized account deletion and eliminating client-side privileged operations.
Output: Refactored settings account deletion flow + backend delete-account function + tests and static guards.
</objective>

<execution_context>
@/Users/rogerpersonal/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rogerpersonal/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-safety-access-security/01-CONTEXT.md
@.planning/phases/01-data-safety-access-security/01-RESEARCH.md
@lib/features/settings/data/datasources/account_remote_datasource.dart
@lib/features/settings/data/repositories/account_repository_impl.dart
@lib/features/settings/domain/usecases/delete_account.dart
@lib/features/settings/presentation/providers/account_actions_provider.dart
@lib/features/settings/presentation/screens/settings_screen.dart
</context>

<tasks>
<task type="auto">
  <name>Task 1: Refactor delete-account contract to current-auth-user semantics</name>
  <files>lib/features/settings/data/datasources/account_remote_datasource.dart, lib/features/settings/data/repositories/account_repository_impl.dart, lib/features/settings/domain/repositories/account_repository.dart, lib/features/settings/domain/usecases/delete_account.dart, lib/features/settings/presentation/providers/account_actions_provider.dart</files>
  <action>Remove `userId` argument from delete-account interfaces/use cases/provider/UI, and make deletion intent “delete currently authenticated user account”. Keep error mapping behavior and logout flow intact after success.</action>
  <verify>
    <automated>flutter analyze lib/features/settings/data lib/features/settings/domain lib/features/settings/presentation</automated>
  </verify>
  <done>No delete-account call path in Flutter accepts arbitrary userId input.</done>
</task>

<task type="auto">
  <name>Task 2: Implement backend-authorized delete-account endpoint and client invocation</name>
  <files>supabase/functions/delete-account/index.ts, supabase/functions/delete-account/deno.json, lib/features/settings/data/datasources/account_remote_datasource.dart</files>
  <action>Create a Supabase Edge Function that requires JWT, derives actor user from auth context, performs delete via service/admin context server-side, and returns structured error codes. Update datasource to invoke this function via `client.functions.invoke` with no target userId from client payload.</action>
  <verify>
    <automated>rg -n "functions\\.invoke\\('delete-account'\\)|auth\\.admin\\.deleteUser" lib/features/settings/data/datasources/account_remote_datasource.dart</automated>
  </verify>
  <done>Datasource uses backend function invocation and contains no direct admin delete API usage.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests and static security guard</name>
  <files>test/features/settings/data/datasources/account_remote_datasource_test.dart, test/features/settings/presentation/providers/account_actions_provider_test.dart</files>
  <action>Add tests covering successful backend-invoked deletion, backend error propagation, and UI/provider behavior after deletion success/failure. Add static scan assertion in verification to block future reintroduction of client admin delete calls.</action>
  <verify>
    <automated>flutter test test/features/settings/data/datasources/account_remote_datasource_test.dart test/features/settings/presentation/providers/account_actions_provider_test.dart</automated>
  </verify>
  <done>Tests prove backend-only deletion path and fail if client-side admin delete reappears.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] flutter test test/features/settings/data/datasources/account_remote_datasource_test.dart test/features/settings/presentation/providers/account_actions_provider_test.dart
- [ ] flutter analyze lib/features/settings/data lib/features/settings/domain lib/features/settings/presentation
- [ ] bash -lc "! rg -n 'auth\\.admin\\.deleteUser' lib"
- [ ] rg -n \"functions\\.invoke\\('delete-account'\\)\" lib/features/settings/data/datasources/account_remote_datasource.dart
</verification>

<success_criteria>
- All tasks completed
- SECU-02 boundary enforced: account deletion only via authorized backend endpoint
- No privileged admin auth call remains in client code
- Settings delete-account UX/provider flow remains functional
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-safety-access-security/01-02-SUMMARY.md`
</output>
