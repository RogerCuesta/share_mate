---
phase: 01-data-safety-access-security
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/main.dart
  - lib/core/storage/hive_service.dart
  - lib/core/sync/payment_sync_queue.dart
  - lib/core/storage/local_migrations/local_migration.dart
  - lib/core/storage/local_migrations/local_migration_runner.dart
  - lib/core/storage/local_migrations/migrations/v1_non_destructive_baseline_migration.dart
  - lib/core/storage/local_migrations/migrations/v2_encrypt_sensitive_boxes_migration.dart
  - test/core/storage/local_migration_runner_test.dart
  - test/core/storage/encrypted_bootstrap_test.dart
autonomous: true
requirements: [SAFE-01, SAFE-02, SECU-01]
user_setup: []
must_haves:
  truths:
    - "App startup never deletes existing subscription/member/payment/contact data."
    - "Local migrations run once per schema version and are idempotent on repeated startups."
    - "Sensitive business boxes are opened with encryption and never silently downgraded to plaintext."
    - "Locked decision: if encryption-key retrieval fails, startup enters key-failure safe mode (writes blocked, guided recovery shown, no plaintext fallback path)."
  artifacts:
    - "lib/core/storage/local_migrations/local_migration_runner.dart"
    - "lib/core/storage/local_migrations/migrations/v1_non_destructive_baseline_migration.dart"
    - "lib/core/storage/local_migrations/migrations/v2_encrypt_sensitive_boxes_migration.dart"
    - "test/core/storage/local_migration_runner_test.dart"
  key_links:
    - "main.dart bootstrap executes LocalMigrationRunner before opening subscription/payment boxes."
    - "PaymentSyncQueueService and subscription bootstrap both use HiveService encrypted box opening."
    - "Contacts datasource encryption and key-failure safe mode behavior are verified via automated checks and bootstrap tests."
---

<objective>
Implement non-destructive, versioned local migration flow and enforce encryption at rest for Phase 1 business boxes.

Purpose: Close SAFE-01/SAFE-02/SECU-01 by replacing destructive startup logic with deterministic migrations and encrypted storage paths.
Output: Migration runner + concrete migrations + encrypted startup wiring + regression tests.
</objective>

<execution_context>
@/Users/rogerpersonal/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rogerpersonal/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-safety-access-security/01-CONTEXT.md
@.planning/phases/01-data-safety-access-security/01-RESEARCH.md
@lib/main.dart
@lib/core/storage/hive_service.dart
@lib/core/sync/payment_sync_queue.dart
@lib/features/subscriptions/data/datasources/subscription_local_datasource.dart
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add versioned non-destructive migration primitives</name>
  <files>lib/core/storage/local_migrations/local_migration.dart, lib/core/storage/local_migrations/local_migration_runner.dart</files>
  <action>Create a migration contract and runner backed by a dedicated metadata box (schema version, in-progress, last successful migration, failure code). Execute migrations in ascending version order, skip already-applied versions, and fail closed without deleting any existing business box data.</action>
  <verify>
    <automated>flutter test test/core/storage/local_migration_runner_test.dart</automated>
  </verify>
  <done>Runner persists migration state, applies pending migrations exactly once per version, and preserves existing rows on migration failure.</done>
</task>

<task type="auto">
  <name>Task 2: Replace destructive startup migration and enforce encrypted opens</name>
  <files>lib/main.dart, lib/core/sync/payment_sync_queue.dart</files>
  <action>Remove `_migrateSubscriptionBoxes` delete-box logic from bootstrap, run the new migration runner during startup, and open subscriptions/subscription_members/payment_history/payment_sync_queue/contacts using encrypted Hive paths. Keep startup order deterministic: init Hive, run migrations, open boxes, build app. Lock decision: if key retrieval fails, enter key-failure safe mode that blocks writes, routes users to guided recovery, and forbids plaintext fallback opens.</action>
  <verify>
    <automated>flutter analyze lib/main.dart lib/core/sync/payment_sync_queue.dart lib/features/contacts/data/datasources/contact_local_datasource.dart</automated>
    <automated>bash -lc "rg -n \"_contactsBoxName|_myContactsCacheBoxName|encrypted:\\s*true\" lib/features/contacts/data/datasources/contact_local_datasource.dart && ! rg -n \"_migrateSubscriptionBoxes|deleteBox\\(SubscriptionLocalDataSourceImpl\\.(subscriptionsBoxName|membersBoxName)\" lib/main.dart"</automated>
  </verify>
  <done>Startup no longer deletes subscription/member boxes, sensitive opens in bootstrap/sync queue/contacts are encrypted, and key-failure safe mode blocks writes with guided recovery and no plaintext fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Implement concrete migrations for baseline safety and encryption conversion</name>
  <files>lib/core/storage/local_migrations/migrations/v1_non_destructive_baseline_migration.dart, lib/core/storage/local_migrations/migrations/v2_encrypt_sensitive_boxes_migration.dart, test/core/storage/encrypted_bootstrap_test.dart</files>
  <action>Add `v1` baseline migration to establish metadata idempotently and `v2` migration to move plaintext sensitive boxes to encrypted boxes using copy-validate-swap with temporary backup boxes. Scope the first-wave migration strictly to `subscriptions`, `subscription_members`, `payment_history`, and `payment_sync_queue`; treat contacts as encrypted-open verification only in this phase. Add tests for first run, second startup idempotency, failure rollback safety, and key-failure safe-mode behavior (writes blocked + guided recovery + no plaintext fallback).</action>
  <verify>
    <automated>flutter test test/core/storage/local_migration_runner_test.dart test/core/storage/encrypted_bootstrap_test.dart</automated>
    <automated>bash -lc "rg -n \"key-failure safe mode|guided recovery|plaintext fallback\" test/core/storage/encrypted_bootstrap_test.dart"</automated>
  </verify>
  <done>Legacy plaintext data survives migration for the four first-wave boxes (`subscriptions`, `subscription_members`, `payment_history`, `payment_sync_queue`), contacts encryption remains explicitly verified at open-time, key-failure safe mode is regression-tested, and rerunning startup causes zero duplicate/destructive mutations.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] flutter test test/core/storage/local_migration_runner_test.dart test/core/storage/encrypted_bootstrap_test.dart
- [ ] flutter analyze lib/main.dart lib/core/storage/hive_service.dart lib/core/sync/payment_sync_queue.dart lib/features/contacts/data/datasources/contact_local_datasource.dart
- [ ] bash -lc "! rg -n '_migrateSubscriptionBoxes|deleteBox\\(SubscriptionLocalDataSourceImpl\\.(subscriptionsBoxName|membersBoxName)' lib/main.dart"
- [ ] bash -lc "rg -n \"_contactsBoxName|_myContactsCacheBoxName|encrypted:\\s*true\" lib/features/contacts/data/datasources/contact_local_datasource.dart"
</verification>

<success_criteria>
- All tasks completed
- SAFE-01 validated by data-preservation tests (no startup deletion path remains)
- SAFE-02 validated by versioned/idempotent migration tests
- SECU-01 validated by encrypted open paths (including contacts), migration parity checks, and key-failure safe mode with no plaintext fallback
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-safety-access-security/01-01-SUMMARY.md`
</output>
