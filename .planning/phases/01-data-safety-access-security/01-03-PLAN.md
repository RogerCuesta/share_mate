---
phase: 01-data-safety-access-security
plan: "03"
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - supabase/migrations/20251225_payment_history_enhancements.sql
  - supabase/migrations/20260308_phase1_rls_hardening.sql
  - docs/SUPABASE_SCHEMA.sql
  - scripts/security/rls_policy_audit.sql
  - scripts/security/run_rls_policy_audit.sh
  - test/security/rls_policy_audit_test.dart
autonomous: true
requirements: [SECU-03]
user_setup: []
must_haves:
  truths:
    - "Business tables enforce per-user read/write isolation via RLS policies."
    - "SECURITY DEFINER routines touching business data explicitly validate caller ownership."
    - "Security audits provide repeatable proof that cross-user access is denied."
  artifacts:
    - "supabase/migrations/20260308_phase1_rls_hardening.sql"
    - "scripts/security/rls_policy_audit.sql"
    - "test/security/rls_policy_audit_test.dart"
  key_links:
    - "RLS hardening migration and payment-history function updates are validated by policy-audit SQL checks."
    - "Security test harness maps failing checks directly to SECU-03 release gates."
---

<objective>
Harden Supabase tenant isolation guarantees and add executable SECU-03 proof checks.

Purpose: Ensure business data tables and privileged SQL functions are safe by default and verifiably isolated per user.
Output: Canonical RLS hardening migration, SECURITY DEFINER safeguards, and repeatable policy audit checks.
</objective>

<execution_context>
@/Users/rogerpersonal/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rogerpersonal/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-safety-access-security/01-CONTEXT.md
@.planning/phases/01-data-safety-access-security/01-RESEARCH.md
@supabase/migrations/20251225_payment_history_enhancements.sql
@supabase/migrations/20260108_contacts_refactor.sql
@docs/SUPABASE_SCHEMA.sql
</context>

<tasks>
<task type="auto">
  <name>Task 1: Author canonical RLS hardening migration for business tables</name>
  <files>supabase/migrations/20260308_phase1_rls_hardening.sql, docs/SUPABASE_SCHEMA.sql</files>
  <action>Create a new migration that ensures `subscriptions`, `subscription_members`, `payment_history`, and `contacts` have RLS enabled and explicit CRUD ownership policies. Keep migration idempotent (`IF EXISTS/IF NOT EXISTS` or `DROP POLICY IF EXISTS + CREATE POLICY`) and document final policy state in schema docs.</action>
  <verify>
    <automated>rg -n "ALTER TABLE (subscriptions|subscription_members|payment_history|contacts) ENABLE ROW LEVEL SECURITY|CREATE POLICY" supabase/migrations/20260308_phase1_rls_hardening.sql</automated>
  </verify>
  <done>All Phase 1 business tables have explicit, replayable RLS policy definitions in migration source-of-truth.</done>
</task>

<task type="auto">
  <name>Task 2: Harden SECURITY DEFINER payment-history functions</name>
  <files>supabase/migrations/20251225_payment_history_enhancements.sql, supabase/migrations/20260308_phase1_rls_hardening.sql</files>
  <action>Update payment-history RPC functions to set deterministic `search_path` and enforce caller authorization checks (`auth.uid()` ownership path) before data mutation/reads. Preserve function signatures expected by client code while rejecting cross-tenant row access.</action>
  <verify>
    <automated>rg -n "SECURITY DEFINER|SET search_path|auth\\.uid\\(\\)" supabase/migrations/20251225_payment_history_enhancements.sql supabase/migrations/20260308_phase1_rls_hardening.sql</automated>
  </verify>
  <done>Privileged functions contain explicit authorization guards and stable search_path configuration.</done>
</task>

<task type="auto">
  <name>Task 3: Add runnable SECU-03 policy audit checks</name>
  <files>scripts/security/rls_policy_audit.sql, scripts/security/run_rls_policy_audit.sh, test/security/rls_policy_audit_test.dart</files>
  <action>Add SQL audit queries and a thin runner script that verify RLS enabled/policies present and simulate forbidden cross-user access checks. Add a test wrapper that fails on missing policies or insecure function definitions so SECU-03 can be gated before release.</action>
  <verify>
    <automated>bash scripts/security/run_rls_policy_audit.sh</automated>
  </verify>
  <done>Audit command exits non-zero on any isolation gap and produces clear output mapped to SECU-03.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] bash scripts/security/run_rls_policy_audit.sh
- [ ] flutter test test/security/rls_policy_audit_test.dart
- [ ] rg -n "SECU-03|RLS|policy" .planning/phases/01-data-safety-access-security/01-03-SUMMARY.md || true
</verification>

<success_criteria>
- All tasks completed
- SECU-03 enforced by migration-level RLS policy definitions for all business tables in scope
- SECURITY DEFINER routines reject unauthorized cross-user operations
- Automated audit checks are executable and fail fast on isolation regressions
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-safety-access-security/01-03-SUMMARY.md`
</output>
