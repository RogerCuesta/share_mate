---
phase: 03-subscription-setup-flow
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260309_phase3_subscription_setup_foundation.sql
  - lib/features/subscriptions/domain/entities/service_template.dart
  - lib/features/subscriptions/data/models/service_template_model.dart
  - lib/features/subscriptions/domain/entities/subscription.dart
  - lib/features/subscriptions/data/models/subscription_model.dart
  - lib/features/subscriptions/data/models/subscription_member_model.dart
  - lib/features/contacts/domain/entities/contact.dart
  - lib/features/contacts/domain/entities/add_contact_input.dart
  - lib/features/contacts/domain/entities/update_contact_input.dart
  - lib/features/contacts/data/models/contact_model.dart
  - test/features/subscriptions/data/models/subscription_model_test.dart
  - test/features/subscriptions/data/models/subscription_member_model_test.dart
  - test/features/contacts/data/models/contact_model_test.dart
autonomous: true
requirements: [CATA-01, CNTC-01, SPLT-03]
user_setup: []
must_haves:
  truths:
    - "`service_templates` is available behind authenticated read access so catalog search has a canonical backend source."
    - "Local contacts support name + avatar/color without requiring email in domain, data, and persistence contracts."
    - "`subscription_members.user_email` is nullable for local contacts without email, while preserving existing non-null email values."
    - "Subscriptions persist an explicit `billing_anchor_day` so month normalization is deterministic (31 -> 28/29 in Feb, back to 31 in longer months)."
  artifacts:
    - "supabase/migrations/20260309_phase3_subscription_setup_foundation.sql"
    - "lib/features/subscriptions/domain/entities/service_template.dart"
    - "lib/features/contacts/domain/entities/contact.dart"
    - "lib/features/subscriptions/data/models/subscription_model.dart"
  key_links:
    - "Supabase migration schema fields map 1:1 with Flutter models for templates, contacts, and billing anchor day."
    - "Schema-level nullable `subscription_members.user_email` contract is mirrored by subscription member model serialization."
    - "Contact entity/input optional-email contract is preserved through model serialization/deserialization paths."
---

<objective>
Build Phase 3 data-contract foundations so catalog, local-contacts-without-email, and billing-anchor date logic can be implemented safely in later plans.

Purpose: unblock CATA-01, CNTC-01, and SPLT-03 with schema and model contracts that match product decisions.
Output: SQL migration + Flutter domain/data contract updates + regression tests for serialization compatibility.
</objective>

<execution_context>
@/Users/rogerpersonal/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rogerpersonal/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-subscription-setup-flow/03-CONTEXT.md
@.planning/phases/03-subscription-setup-flow/03-RESEARCH.md
@supabase/migrations/20260108_contacts_refactor.sql
@lib/features/subscriptions/data/models/subscription_model.dart
@lib/features/contacts/data/models/contact_model.dart
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add phase-3 SQL migration for templates, contact optional-email, and billing anchor day</name>
  <files>supabase/migrations/20260309_phase3_subscription_setup_foundation.sql</files>
  <action>Create a single migration that introduces `service_templates` (search-ready columns + active flag + timestamps + authenticated read policy), relaxes contacts email constraints to allow null while preserving safe uniqueness semantics for non-null emails, updates `subscription_members.user_email` to nullable with safe backfill semantics for existing rows, adds contact color/avatar metadata for quick-create flow, and adds `billing_anchor_day` to subscriptions with backfill for existing rows.</action>
  <verify>
    <automated>bash -lc "rg -n \"create table if not exists service_templates|billing_anchor_day|contact_email|subscription_members|user_email\" supabase/migrations/20260309_phase3_subscription_setup_foundation.sql"</automated>
  </verify>
  <done>Schema and policies exist for catalog search, no-email local contacts (including nullable `subscription_members.user_email`), and deterministic billing anchor persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Update Flutter entities/models to reflect new canonical contracts</name>
  <files>lib/features/subscriptions/domain/entities/service_template.dart, lib/features/subscriptions/data/models/service_template_model.dart, lib/features/subscriptions/domain/entities/subscription.dart, lib/features/subscriptions/data/models/subscription_model.dart, lib/features/subscriptions/data/models/subscription_member_model.dart, lib/features/contacts/domain/entities/contact.dart, lib/features/contacts/domain/entities/add_contact_input.dart, lib/features/contacts/domain/entities/update_contact_input.dart, lib/features/contacts/data/models/contact_model.dart</files>
  <action>Introduce service template domain/model types, make contact email optional across input/entity/model layers while keeping validation for provided emails, add `billingAnchorDay` to subscription domain/model constructors plus JSON/Hive mappings, and make subscription member model serialization/deserialization null-safe for `userEmail` to match the no-email contact contract. Preserve backward compatibility for legacy records that do not yet contain the new fields.</action>
  <verify>
    <automated>flutter analyze lib/features/subscriptions/domain/entities lib/features/subscriptions/data/models lib/features/contacts/domain/entities lib/features/contacts/data/models</automated>
  </verify>
  <done>Domain/data contracts align with migration and support optional email (including nullable subscription member email) + anchor-day without breaking old rows.</done>
</task>

<task type="auto">
  <name>Task 3: Add contract regression tests for optional fields and backfilled defaults</name>
  <files>test/features/subscriptions/data/models/subscription_model_test.dart, test/features/subscriptions/data/models/subscription_member_model_test.dart, test/features/contacts/data/models/contact_model_test.dart</files>
  <action>Add tests that prove legacy payloads deserialize safely, new payloads serialize with the new fields, nullable contact emails are accepted and preserved, nullable `subscription_members.user_email` payloads round-trip safely, and `billingAnchorDay` defaults correctly when absent.</action>
  <verify>
    <automated>flutter test test/features/subscriptions/data/models/subscription_model_test.dart test/features/subscriptions/data/models/subscription_member_model_test.dart test/features/contacts/data/models/contact_model_test.dart</automated>
  </verify>
  <done>Automated tests guard nullable-email and anchor-day contracts (including subscription member email persistence shape) before UI and repository work starts.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] flutter analyze lib/features/subscriptions/domain/entities lib/features/subscriptions/data/models lib/features/contacts/domain/entities lib/features/contacts/data/models
- [ ] flutter test test/features/subscriptions/data/models/subscription_model_test.dart test/features/subscriptions/data/models/subscription_member_model_test.dart test/features/contacts/data/models/contact_model_test.dart
- [ ] bash -lc "rg -n \"service_templates|billing_anchor_day|contact_email|subscription_members|user_email\" supabase/migrations/20260309_phase3_subscription_setup_foundation.sql"
</verification>

<success_criteria>
- All tasks completed
- CATA-01 foundation exists via `service_templates` backend contract
- CNTC-01 foundation exists via optional-email contact contracts
- SPLT-03 foundation exists via persisted `billing_anchor_day`
</success_criteria>

<output>
After completion, create `.planning/phases/03-subscription-setup-flow/03-01-SUMMARY.md`
</output>
