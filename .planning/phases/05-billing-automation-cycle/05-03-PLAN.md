---
phase: 05-billing-automation-cycle
plan: "03"
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - supabase/migrations/20260311_phase5_billing_cycle_reset.sql
  - lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart
  - lib/core/sync/payment_sync_conflict_resolver.dart
  - lib/features/subscriptions/presentation/providers/payment_reconciliation_provider.dart
  - lib/features/subscriptions/presentation/providers/sync_status_provider.dart
  - test/core/sync/conflict_resolution_test.dart
  - test/core/sync/payment_sync_orchestrator_test.dart
  - test/features/subscriptions/presentation/providers/payment_reconciliation_provider_test.dart
autonomous: true
requirements: [BILL-01, BILL-02, BILL-03]
user_setup: []
must_haves:
  truths:
    - "Cycle reset is backend-driven and atomic for subscriptions + member payment state, with auditable reset records."
    - "Monthly and yearly subscriptions reset on their own cycle boundary, not on a fixed global day."
    - "Stale local operations from a previous cycle cannot override backend reset outcomes; backend remains canonical."
    - "Client reconciliation feedback is brief and non-blocking, consistent with Phase 4 sync messaging style."
  artifacts:
    - "supabase/migrations/20260311_phase5_billing_cycle_reset.sql"
    - "lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart"
    - "test/core/sync/conflict_resolution_test.dart"
  key_links:
    - "Reset migration aligns with existing sync conflict preflight (`cycleDueDate`) from Phase 2 architecture."
    - "Reconciliation signal feeds existing subscription/home refresh providers instead of introducing new UI flows."
---

<objective>
Deliver backend monthly-cycle reset contracts and client reconciliation wiring so new-cycle pending states converge deterministically across devices.

Purpose: satisfy BILL-03 with canonical backend reset plus robust client conflict handling.
Output: Supabase migration for cycle reset, client datasource hooks, and sync/reconciliation regressions.
</objective>

<execution_context>
@/Users/rogerpersonal/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rogerpersonal/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-billing-automation-cycle/05-CONTEXT.md
@.planning/phases/05-billing-automation-cycle/05-RESEARCH.md
@supabase/migrations/20260308_phase2_sync_conflict_reconciliation.sql
@lib/core/sync/payment_sync_conflict_resolver.dart
@lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart
@lib/features/subscriptions/presentation/providers/payment_reconciliation_provider.dart
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add backend migration for billing cycle reset RPC, audit log, and scheduler registration</name>
  <files>supabase/migrations/20260311_phase5_billing_cycle_reset.sql</files>
  <action>Create atomic SQL migration that: (a) identifies due active subscriptions by cycle boundary, (b) advances subscription due dates to next valid cycle date respecting billing anchor rules, (c) resets member payment state to pending, and (d) inserts audit rows for each reset batch. Register periodic execution via supported scheduler strategy in migration comments/contracts.</action>
  <verify>
    <automated>bash -lc "rg -n \"create table if not exists billing_cycle_resets|create or replace function|has_paid|pg_cron|grant execute\" supabase/migrations/20260311_phase5_billing_cycle_reset.sql"</automated>
  </verify>
  <done>Backend can execute cycle resets deterministically with auditable side effects required by BILL-03.</done>
</task>

<task type="auto">
  <name>Task 2: Wire client datasource and sync providers to consume reset reconciliation signals</name>
  <files>lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart, lib/features/subscriptions/presentation/providers/sync_status_provider.dart, lib/features/subscriptions/presentation/providers/payment_reconciliation_provider.dart</files>
  <action>Add/reset-aware read contract from backend (RPC or metadata endpoint) and propagate reconciliation reason through existing sync status and payment reconciliation providers so Home/detail refresh and snackbar feedback remain deterministic and non-blocking.</action>
  <verify>
    <automated>flutter analyze lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart lib/features/subscriptions/presentation/providers/sync_status_provider.dart lib/features/subscriptions/presentation/providers/payment_reconciliation_provider.dart</automated>
  </verify>
  <done>Client reflects backend cycle resets quickly and communicates corrections without blocking the workflow.</done>
</task>

<task type="auto">
  <name>Task 3: Extend sync conflict tests for stale-cycle no-op and reset reconciliation</name>
  <files>lib/core/sync/payment_sync_conflict_resolver.dart, test/core/sync/conflict_resolution_test.dart, test/core/sync/payment_sync_orchestrator_test.dart, test/features/subscriptions/presentation/providers/payment_reconciliation_provider_test.dart</files>
  <action>Add regression coverage proving queued operations from old cycle are terminalized as cycle conflict no-op and that reconciliation metadata is emitted for UI feedback. Keep existing terminal/pending status precedence guarantees intact.</action>
  <verify>
    <automated>flutter test test/core/sync/conflict_resolution_test.dart test/core/sync/payment_sync_orchestrator_test.dart test/features/subscriptions/presentation/providers/payment_reconciliation_provider_test.dart</automated>
  </verify>
  <done>BILL-03 conflict convergence is locked by tests and remains compatible with Phase 2/4 sync contracts.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] flutter test test/core/sync/conflict_resolution_test.dart test/core/sync/payment_sync_orchestrator_test.dart test/features/subscriptions/presentation/providers/payment_reconciliation_provider_test.dart
- [ ] flutter analyze lib/features/subscriptions/data/datasources/subscription_remote_datasource.dart lib/core/sync/payment_sync_conflict_resolver.dart lib/features/subscriptions/presentation/providers/payment_reconciliation_provider.dart lib/features/subscriptions/presentation/providers/sync_status_provider.dart
- [ ] bash -lc "rg -n \"billing_cycle_resets|cycle_conflict_noop|reconciliation_reason|has_paid=false|due_date\" supabase/migrations/20260311_phase5_billing_cycle_reset.sql lib/core/sync/payment_sync_conflict_resolver.dart lib/features/subscriptions/presentation/providers"
</verification>

<success_criteria>
- All tasks completed
- BILL-03 satisfied with backend-owned cycle reset and deterministic client reconciliation
- Existing SYNC-02 conflict behavior is preserved and extended for billing automation cycle resets
</success_criteria>

<output>
After completion, create `.planning/phases/05-billing-automation-cycle/05-03-SUMMARY.md`
</output>
