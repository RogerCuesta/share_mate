---
phase: 05-billing-automation-cycle
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/billing_automation/domain/models/billing_reminder_plan.dart
  - lib/features/billing_automation/domain/services/billing_reminder_scheduler.dart
  - lib/features/billing_automation/data/local/billing_reminder_registry.dart
  - lib/features/billing_automation/data/platform/local_notification_adapter.dart
  - lib/core/di/injection.dart
  - lib/routing/app_router.dart
  - test/features/billing_automation/domain/billing_reminder_scheduler_test.dart
  - test/features/billing_automation/data/billing_reminder_registry_test.dart
autonomous: true
requirements: [BILL-01, BILL-02, BILL-03]
user_setup: []
must_haves:
  truths:
    - "Reminder scheduling is deterministic: one reminder per `(subscriptionId, cycleDueDate)` at dueDate-24h in local timezone."
    - "Inactive/cancelled subscriptions never keep pending reminders in registry or platform scheduler."
    - "Notification payload deep-links to subscription detail using existing router contracts."
    - "Scheduler foundations are test-covered before app lifecycle triggers are introduced."
  artifacts:
    - "lib/features/billing_automation/domain/services/billing_reminder_scheduler.dart"
    - "lib/features/billing_automation/data/local/billing_reminder_registry.dart"
    - "test/features/billing_automation/domain/billing_reminder_scheduler_test.dart"
  key_links:
    - "Reminder scheduler consumes subscription dueDate + billingCycle semantics already established in subscriptions domain."
    - "Notification route target reuses subscription detail navigation path in `app_router.dart`."
---

<objective>
Establish the billing automation core for local T-24h reminders with deterministic scheduling, dedupe keying, and deep-link payload contracts.

Purpose: deliver the technical base for BILL-01 and prevent duplicate-risk regressions before lifecycle orchestration.
Output: reminder model/service, registry persistence, platform adapter contract, and unit tests.
</objective>

<execution_context>
@/Users/rogerpersonal/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rogerpersonal/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-billing-automation-cycle/05-CONTEXT.md
@.planning/phases/05-billing-automation-cycle/05-RESEARCH.md
@.planning/phases/05-billing-automation-cycle/05-VALIDATION.md
@lib/features/subscriptions/domain/entities/subscription.dart
@lib/routing/app_router.dart
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add reminder planning model and T-24h schedule computation</name>
  <files>lib/features/billing_automation/domain/models/billing_reminder_plan.dart, lib/features/billing_automation/domain/services/billing_reminder_scheduler.dart, test/features/billing_automation/domain/billing_reminder_scheduler_test.dart</files>
  <action>Create reminder planning entities/services that derive one notification candidate per active subscription cycle with timestamp exactly 24h before due date in local timezone. Exclude already-past reminders and keep deterministic keying by `(subscriptionId, cycleDueDate)`.</action>
  <verify>
    <automated>flutter test test/features/billing_automation/domain/billing_reminder_scheduler_test.dart</automated>
  </verify>
  <done>Scheduler produces stable reminder plans and guards month/year boundary edge cases required for BILL-01.</done>
</task>

<task type="auto">
  <name>Task 2: Implement registry and platform adapter contracts with idempotent diff semantics</name>
  <files>lib/features/billing_automation/data/local/billing_reminder_registry.dart, lib/features/billing_automation/data/platform/local_notification_adapter.dart, test/features/billing_automation/data/billing_reminder_registry_test.dart</files>
  <action>Add local registry storage for scheduled reminders (subscriptionId, cycleDueDate, notificationId, scheduledAt, timezoneId) and platform adapter APIs for schedule/cancel. Implement create/keep/cancel diff behavior so repeated runs are idempotent.</action>
  <verify>
    <automated>flutter test test/features/billing_automation/data/billing_reminder_registry_test.dart</automated>
  </verify>
  <done>Reminder persistence and cancellation rules prevent critical duplicates across repeated orchestration runs.</done>
</task>

<task type="auto">
  <name>Task 3: Wire DI registration and deep-link payload format</name>
  <files>lib/core/di/injection.dart, lib/routing/app_router.dart, lib/features/billing_automation/domain/services/billing_reminder_scheduler.dart</files>
  <action>Register scheduler/registry/adapter in dependency injection and define notification payload contract that opens existing subscription detail route (`/subscription/:id`) when user taps a reminder.</action>
  <verify>
    <automated>flutter analyze lib/features/billing_automation/domain lib/features/billing_automation/data lib/core/di/injection.dart lib/routing/app_router.dart</automated>
  </verify>
  <done>Core billing reminder module is injectable and route-compatible for downstream lifecycle orchestration.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] flutter test test/features/billing_automation/domain/billing_reminder_scheduler_test.dart test/features/billing_automation/data/billing_reminder_registry_test.dart
- [ ] flutter analyze lib/features/billing_automation/domain lib/features/billing_automation/data lib/core/di/injection.dart lib/routing/app_router.dart
- [ ] bash -lc "rg -n \"subscriptionId|cycleDueDate|24h|timezone|notificationId\" lib/features/billing_automation test/features/billing_automation"
</verification>

<success_criteria>
- All tasks completed
- BILL-01 has deterministic local scheduler foundations with automated coverage
- BILL-02 duplicate prevention primitives exist via idempotent registry diffing
</success_criteria>

<output>
After completion, create `.planning/phases/05-billing-automation-cycle/05-01-SUMMARY.md`
</output>
