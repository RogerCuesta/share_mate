# Local Skills

This folder should only contain complete local skills (each with `SKILL.md`).

Current local skill:
- `ui-ux-pro-max/`

Notes:
- Empty placeholder skill directories were removed to avoid false discovery and load errors.
- Global skills remain available from `$CODEX_HOME/skills` when configured in the session.
